Clone the front-end template from IBM:

git clone https://github.com/ibm-developer-skills-network/LLM_application_chatbot

Copy app.py from this repo into that folder:

cp app.py LLM_application_chatbot/

Install dependencies and run...
